# 待修改事項（程式漏洞盤點）

> 2026-07-27 全面檢查。聚焦「會產生錯誤行為的 bug」；策略調參項目仍歸 `docs/TODO.md`，不重複。
> 每項附：症狀 → 根因（檔案:行）→ 修改方向 → 驗證方式。

## 進度（2026-07-27）

已修：P0-1①②（上傳過濾+update-only）、P0-2（靜音開關+文案）、P1-1（推播 user_id 檢查）、
P1-2（opened_at 覆寫）、P1-3（SignalCard 按鈕改拉取）、P2-1（webhook production fail-closed）、
P2-2（刪除重試佇列）、P2-3（LINE 全拔除，獨立考量後與其他項目合併一次 commit——interleave
太深，硬拆風險大於好處）、P2-4（settings 頁顯示 build SHA）。tsc + vitest(105) + build 全過。

**已修（2026-07-28，`9bd5d61`）**：
- P0-1③ 取消不再 DELETE 整列，改 `status='cancelled'` + `result='CANCELLED'` +
  `closed_at=now` 軟刪。詳見 `docs/TODO.md` P1 #0b。

**已確認不用做**：
- ~~P1-1 一次性 SQL 清理（`user_id IS NULL` 的殘留列）~~ —— 2026-07-27 查證：目前 33 筆交易全部只有單一 user_id，無 NULL 殘留，這條防呆已經沒有清理對象。

**過程中發現、已個別處理的東西**：
- `removeCoin` 沒刪 DB 列，下次 `loadFromSupabase` 會把移除的監控幣種撿回來（跟本次修的殭屍單同一種「client 沒刪乾淨」模式，但範圍是 watchlist 不是 trades）——已修，見 commit `55c5bc4`。
- status=NULL 的真正根因（DB 缺欄位）、AKE 秒成交排查（cron-job.org 排程被停用超過一週）、市場入場重複推播——三件事的完整診斷過程與修法都記在下方「2026-07-27 更正」與相關 commit，不在此重複列出。

---

## 0. 本次回報「幽靈撤單」的完整解釋鏈

「明明沒有掛那單，卻推播『逾期 4 根 K 線未成交，掛單取消』」是**一個設計事實 + 一個真 bug** 疊加的結果：

1. **設計事實：推薦單 = 自動掛單**。`route.ts:1894-1930` — 每張達標的 A/B 級訊號都會**自動**寫入 `trades` 表（限價單寫 `status='waiting'`）。你從來不需要按任何鍵，「推薦單」發出的那一刻，系統已經替你記了一張掛單。4 根 K 線沒回測到進場價 → 伺服器照規格 §3-A 取消 → 推播。所以「我沒掛單」是對的——**是系統掛的**。訊號推播跟掛單取消推播中間隔了最多 4 小時（1h 時框），半夜的訊號推播被滑掉之後，早上只看到取消推播，就是「沒頭沒尾」的體感。→ 修改方向見 **P0-2**。

2. **真 bug：客戶端 upsert 會把伺服器已刪除的掛單「復活」**，同一張單被重複取消、重複推播。→ 見 **P0-1**，這是本清單最重要的一項。

---

## P0-1 客戶端 upsert 復活已刪除的掛單（統一根因，高優先）

**一個機制，同時解釋三個懸案：**

| 已觀察到的症狀 | 如何被此機制產生 |
|---|---|
| 「一直顯示掛單取消」（重複推播） | 復活的殭屍列 `opened_at` 是舊的 → 下一輪 cron 立刻再判逾期 → 再刪、再推播。`sw.js` 的 `renotify: true` + 同 tag → 手機每次都重新震動 |
| ~~`status=NULL` 的神祕單~~ **← 此列推論已被推翻，見下方「2026-07-27 更正」** | ~~客戶端上傳的 `baseRow` 不含 status/signal_price → upsert 在列已被刪除時=INSERT → 新列 status 正是 NULL~~ |
| 「剛掛單就直接入場」（秒成交） | 復活列被 NULL→waiting 池撿走 → Phase 1 從舊 `opened_at` 開始掃 K 線 → 進場價早就被觸過 → 立刻判成交、推播「掛單成交」 |

**機制**（`StoreHydration.tsx`）：

- `saveToSupabase` line 572-573：`nonWaiting = trades.filter(t => t.status !== 'waiting')` — **status 為 `undefined`（未確認）的單會通過這個過濾**，被當 openRows 上傳。修掉「捏造 active」之後新單的 status 就是 undefined；修之前更糟——直接被捏成 'active'，一樣通過。
- line 584：`supabase.from('trades').upsert(rows, { onConflict: 'id' })` — **upsert 在目標列不存在時等於 INSERT**。伺服器取消掛單是整列 DELETE（`route.ts:448`），所以「伺服器剛刪 → 客戶端 4 秒 debounce save 撞上」= 原樣復活，而且只帶 base 欄位。
- `fullSyncFromSupabase` line 402-439 同病：`!sv`（伺服器沒有此列）→ 判定「新的本地單」→ 上傳。手動按「同步紀錄」也會復活。
- 復活窗口有多寬：`reconcileFromServer` 失敗時（webhookSecret 還沒載入、401、斷網）status 永遠停在 undefined，每次 debounce save / 10 分鐘定期 save / 切回分頁的 visibility save 都在重複上傳 → 不是罕見競態，是**常開的窗**。

**修改方向**（由小到大，做第 1 項就能止血）：

1. **止血**：上傳過濾改成「只上傳伺服器確認過的非掛單」——`saveToSupabase` 與 `fullSyncFromSupabase` 的過濾條件加 `t.statusConfirmed === true`。未確認的單本來就是伺服器剛建的，客戶端沒有任何需要回寫的東西。
2. **根治**：客戶端對 `trades` 表**取消 INSERT 權**（概念上）——手動新增已移除，所有單都源自伺服器，客戶端只需要 UPDATE（entry_notes、手動平倉結果）。把 upsert 拆成逐筆 `.update().eq('id', …)`，列不存在就靜默跳過，天然不可能復活。
3. **防禦縱深**（接自動化交易前必做，`docs/TODO.md` 已列）：取消不再 DELETE 整列，改 `status='cancelled'` 軟刪——就算有殘餘寫入路徑，也只是改一張已取消的單，不會再進監控池。

**驗證**：修完後在 Supabase 跑
`SELECT id, symbol, status, opened_at FROM trades WHERE status IS NULL AND result IS NULL;`
應該永遠是空的；再觀察一週有沒有「同一 symbol 重複掛單取消推播」。

---

### 2026-07-27 更正：status=NULL 的真正來源是 **DB 缺欄位**，不是客戶端復活

症狀回報：「等待進場分頁看不到單，但有收到掛單通知」。查到的實證鏈：

1. `/api/trade-status`（service role）對 AKEUSDT 回 `{status: null, signalPrice: null}` —— 該列 status 確實是 NULL。
2. 該列建立時間（07-27 11:04）**晚於** P0-1 update-only 修正部署（10:52）→ 客戶端當時已無 INSERT 能力，**復活理論排除**。
3. 該列帶有 `tier='A'`。三條 insert 路徑中，只有最深層 fallback 會剝掉 `status`；它同時也剝掉 `tier`——但 `tier` 欄位有 `DEFAULT 'A'`，所以剝掉後仍會是 'A'。**指紋吻合最深層 fallback**。
4. 最深層 fallback 觸發的前提是 stage 1（只剝 tier/score_breakdown）也失敗 → `strategy / regime / confidence / funding_rate / suggested_risk_pct / suggested_leverage` 之中至少一個欄位在 DB 不存在。
5. `/api/trade-status` 能成功 select `status, signal_price, current_stop` → **這三個欄位存在**，缺的是上面那批 v2 欄位。
6. 最深層 fallback **成功時完全沒有 log**（`route.ts` 該分支只有 `insertOk = true`）→ 已經無聲觸發好幾週。BTCUSDT 那筆也是同一批，只是後來被監控的成交寫入補上了 status，看起來才「正常」。

**結論**：每一張新單都是 status=NULL 出生。之所以還能運作，全靠 `ecc40e6` 把 NULL 併入 waiting 池。

**已修（commit `6c311cf`）**：
- `resolveStatus` 把 `serverStatus===null` 正規化成 `'waiting'`，與伺服器同語意——這是「等待進場看不到單」的直接原因（昨天 P0-1 實作時偏離了原本規劃的做法）。
- 最深層 fallback 成功時改為 `console.error` 大聲記錄，並印出原始錯誤訊息。

**未修，需要你在 Supabase SQL Editor 執行**（正式環境 DB 寫入不由我代跑）：

```sql
ALTER TABLE trades
  ADD COLUMN IF NOT EXISTS strategy           TEXT,
  ADD COLUMN IF NOT EXISTS regime             TEXT,
  ADD COLUMN IF NOT EXISTS confidence         NUMERIC,
  ADD COLUMN IF NOT EXISTS funding_rate       NUMERIC,
  ADD COLUMN IF NOT EXISTS suggested_risk_pct NUMERIC,
  ADD COLUMN IF NOT EXISTS suggested_leverage NUMERIC,
  ADD COLUMN IF NOT EXISTS tier               TEXT DEFAULT 'A',
  ADD COLUMN IF NOT EXISTS score_breakdown    JSONB;

-- 補救既有的 NULL 列（未平倉的當掛單處理，與伺服器語意一致）
UPDATE trades SET status = 'waiting' WHERE status IS NULL AND result IS NULL;
```

跑完之後，新單會直接帶正確的 `status`/`signal_price`，不再依賴 NULL→waiting 的防呆；
`signal_price` 回來也讓限價單成交判定與倉位顯示恢復完整（目前是 NULL，成交判定的
`sp === 0` 分支等於在降級運作）。

---

## P0-2 「推薦單＝自動掛單」的期望落差（設計決策，要你拍板）

系統現在替每張達標訊號自動記掛單、自動取消、每步都推播。接幣安自動化後這行為是對的（真的會掛單），但現階段的體感是「一堆我沒同意過的單在被取消」。三個方向擇一：

- **A. 只改文案（最小）**：取消推播從「⚠️ 掛單取消」改成「推薦單失效（未進場）」，明示這是系統的紙上單，不是你的單。成本 5 分鐘。
- **B. 加靜音開關**：設定頁加「只推播成交與平倉」選項，過期取消不推（漏斗照記）。
- **C. 改 opt-in 入帳**：訊號只推播不入帳，你按了才記。**不建議**——勝率統計、影子模擬、拒絕漏斗全建立在「全量自動入帳」上，改了統計就斷了，而且跟未來自動化交易方向相反。

建議 A+B 都做。

---

## P1-1 監控不過濾 user_id，殘留列的推播打給現在的你

**根因**：`route.ts:228-232` — monitor 撈單刻意不過濾 user_id（單人系統假設），但推播全打給解析出來的單一 `profileId`。客戶端讀單卻是 `.eq('user_id', userId)`（`StoreHydration.tsx:109`）。**任何 user_id 為 NULL 或舊帳號 UUID 的列**：App 裡永遠看不到，伺服器卻照樣監控、照樣推播成交/取消——真正意義的幽靈單。7/17 的註解已實錘「legacy rows with NULL user_id」存在過。

**修改方向**：
1. 一次性清理：`UPDATE trades SET user_id = '<你的 UUID>' WHERE user_id IS NULL;`
2. monitor 撈單加 `.or('user_id.eq.' + profileId + ',user_id.is.null')`，或至少推播前檢查 `trade.user_id`。

**驗證**：`SELECT DISTINCT user_id FROM trades;` — 應該只有一個 UUID，不該有 NULL。

---

## P1-2 42703 fill fallback 覆寫 opened_at，破壞時間錨

**根因**：`route.ts:410` — 主寫入路徑正確地寫 `filled_at` 保留 `opened_at`；但 42703 fallback 寫的是 `{ status:'active', opened_at: filledAt }`——**把掛單時間改成成交時間**。`opened_at` 是 Phase 1 時間錨與 24h/72h/168h 時間止損的基準，被覆寫後時間止損整段後移。只在舊 schema（無 filled_at 欄位）觸發，但一觸發就是靜默的錯誤資料。

**修改方向**：fallback 只寫 `status`，不動 `opened_at`；時間止損在無 `filled_at` 時本來就 fallback 到 `opened_at`，寧可提早關也不要憑空延長。

---

## P1-3 SignalCard「記錄」按鈕殘留的客戶端建單路徑

**根因**：`SignalCard.tsx:19,36` — `addTrade` 仍會在本地建一張 client-ID 的單。伺服器早就自動入帳了，這條路徑跟剛移除的 addManualTrade 同類：靠 signalId 去重 + DB 唯一索引擋著，但時序不對時會閃現一張本地假單，再被 orphan purge 收走。而且它建的單 `statusConfirmed:false`，正是 P0-1 復活機制的合格彈藥。

**修改方向**：按鈕改純顯示（「已自動記錄 ✓」），`addTrade` 的寫路徑移除；或按下時改成呼叫 `loadFromSupabase` 拉伺服器那張正牌的單。

---

## P2-1 Webhook 驗證 fail-open

`trade-status/route.ts:5-10`（`analyze` 同 pattern）：`WEBHOOK_SECRET` 環境變數沒設時 `checkAuth` 直接放行。現在 production 有設所以沒事，但這是顆地雷——哪天換 Vercel 專案／忘了搬 env，整組 service-role 查詢端點裸奔。
**修改方向**：production（`VERCEL_ENV === 'production'`）下未設 secret 一律 401。

## P2-2 deleteTradePermanently 的 DB 刪除是 best-effort

`StoreHydration.tsx:19-28`：本地一定刪，DB 刪除失敗被靜默吞掉，`sessionDeletedIds` 又不跨 session——失敗的話下次開 App 那張單原地復活。
**修改方向**：刪除失敗至少 toast 提示；或把待刪 ID 存 localStorage 重試。

## P2-3 LINE 殘碼還在熱路徑上

已決定棄用 LINE，但 `route.ts` 每輪掃描仍在判斷 `lineToken/lineUserId`、`sendLineWithRetry` 整套還在。不是 bug，是死重：CPU 額度緊張的情況下每輪白付的分支與 import。
**修改方向**：整段拔除（`src/lib/line.ts`、route.ts 所有 LINE 分支、profiles 的 line_token 讀寫）。獨立一個 commit 方便回滾。

## P2-4 已開著的 PWA 不會自動吃到新版

`public/sw.js` 沒有資產快取（好事），但**已經開在背景的 App 分頁會一直跑舊 bundle 直到被殺掉重開**。昨天修的顯示 bug 若手機上「還是有」，先確認 App 是重新開過的。
**修改方向**：設定頁顯示 build 版本（`NEXT_PUBLIC_BUILD_ID` 或 commit hash 前 7 碼），排查時一眼分辨新舊版；可選加「有新版本」提示。

---

## 驗證用 SQL（Supabase SQL Editor 一次跑完）

```sql
-- P0-1 / P1-1：殭屍列與殘留 user_id
SELECT id, symbol, status, user_id, to_timestamp(opened_at/1000) AS opened
FROM trades WHERE result IS NULL ORDER BY opened_at DESC;

SELECT DISTINCT user_id FROM trades;

-- NULL status 開放單（修完 P0-1 後應恆為空）
SELECT count(*) FROM trades WHERE status IS NULL AND result IS NULL;
```

## 與 docs/TODO.md 的對照

- TODO「查明 insert fallback 被觸發的原因」→ **P0-1 提供了更可能的答案**（客戶端 upsert 復活，不是 insert fallback），修完 P0-1 可觀察該項是否就此絕跡。
- TODO「取消不要再刪 DB 列（改 status='cancelled'）」→ 即 P0-1 修改方向第 3 項，優先級應提前。
- 其餘策略調參項（時間止損影子模擬、B 級門檻等）不在本清單範圍。
