# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
# 錨定在根目錄：沒有斜線前綴的 `build/` 會 match 任何深度叫 build 的目錄。
# 2026-08-22 實際踩到——src/app/api/build/route.ts 被靜默忽略，commit 過了但
# 檔案沒進版控，部署後那個端點 404，而呼叫端是「取不到就當沒有新版」，
# 於是整個功能靜默失效。
/build/

# env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# 手動貼金鑰用的暫存檔。副檔名 .txt 不在上面任何一條規則裡，會以未追蹤狀態
# 出現在 git status，下一次 git add -A 就進版控了。正式的設定檔請用 .env.local。
env.txt
*.env.txt
# 對帳報告含 trade_id 與實際損益，不需要進版控。
audit-fabricated-exits-*.json

# debug
npm-debug.log*

# misc
.DS_Store
*.pem
.idea/
.vscode/

# project internals
.expo/
.claude/
.vercel

# typescript incremental build cache
*.tsbuildinfo

# porting reference repos (do not commit)
reference/
